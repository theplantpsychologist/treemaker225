/** Always red, never themed by the active shape's accent — see
 * `constants/shapeTheme.ts` for the shape-driven colors (flap/river/accent). */
export const COLOR_OVERLAP = '#db4437'
export const COLOR_SELECTED = '#dc2626'
export const COLOR_DELETE = '#dc2626'
